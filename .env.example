# Firebase Authentication (server-side REST integration)
FIREBASE_WEB_API_KEY=

# Keep false until Firebase Email/Password provider and FIREBASE_WEB_API_KEY are configured in Vercel.
NEXT_PUBLIC_AUTH_ENABLED=false
